spring:
  datasource:
    url: jdbc:sqlserver://morfeusz;databaseName=dodudek
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServer2012Dialect
        format_sql: true
server:
  port: 9999